<% if users.any? %>
    <div class="col-md-12">
      <ul>
        <% users.each do |user| %>
          <li class="user_list"><%= link_to gravatar_for(user, size: 100), user %>
          <span class="user_text"><%= user.username %></span></li>
        <% end %>
      </ul>
    </div>
<% end %>