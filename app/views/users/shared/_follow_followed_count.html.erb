<% @user ||= current_user %>
<div class="list-group">
    <a href="<%= followings_user_path(@user) %>" class="list-group-item"><strong><%= @user.following_users.count %></strong> フォロー</a>
    <a href="<%= followers_user_path(@user) %>" class="list-group-item"><strong><%= @user.follower_users.count %></strong> フォロワー</a>
</div>